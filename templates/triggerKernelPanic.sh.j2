#!/usr/bin/env bash
set -euo pipefail

echo "DANGER! THIS WILL CAUSE THIS NODE TO KERNEL PANIC!"
echo "DO NOT RUN ON A PRODUCTION SYSTEM!!!"
echo ""
read -p "Are you sure? " -n 1 -r
echo ""

if [[ ! ${REPLY} =~ ^[Yy]$ ]] ; then
    [[ "$0" = "$BASH_SOURCE" ]] && exit 1 || return 1
fi

echo ""

echo 1 > /proc/sys/kernel/sysrq
echo c > /proc/sysrq-trigger
